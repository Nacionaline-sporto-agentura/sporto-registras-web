import { Route, Routes } from 'react-router-dom';

function App() {
  return (
    <Routes>
      <Route path="*" element={<div>Sporto Registras WEB</div>} />
    </Routes>
  );
}

export default App;
